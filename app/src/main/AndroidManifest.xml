<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.samgoggin.labourfinder">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".workerProfileActivity"></activity>
        <activity android:name=".workerRegisterActivity" />
        <activity android:name=".workerLoginActivity" />
        <activity android:name=".startingActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>


        <activity android:name=".JobDetailsActivity" />
        <activity android:name=".InsertJobPostActivity" />
        <activity android:name=".PostJobActivity" />
        <activity android:name=".AllJobActivity" />
        <activity android:name=".HomeActivity2" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".MainActivity">

        </activity>
    </application>

</manifest>